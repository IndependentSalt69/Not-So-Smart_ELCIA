# Docs

Active documentation for the project lives here as it's written.

`archive/` holds implementation logs and audit reports written during the
ELCIA hackathon build (dashboard/backend integration phases, deployment
audits, video pipeline debugging). They're a useful historical record of
*why* certain decisions were made, but they document a point-in-time state,
not the current one - don't treat them as current reference docs.